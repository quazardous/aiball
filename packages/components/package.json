{
  "name": "@quazardous/aiball-components",
  "version": "0.1.0",
  "description": "Read-only component inventory + version extractors driven by components.yaml. Sibling of aiball, no code coupling.",
  "private": true,
  "type": "module",
  "bin": {
    "components": "./bin/components"
  },
  "scripts": {
    "start": "tsx src/cli.ts"
  },
  "dependencies": {
    "commander": "^14.0.3",
    "yaml": "^2.6.0"
  },
  "engines": {
    "node": ">=20"
  }
}
