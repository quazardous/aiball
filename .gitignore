node_modules/
dist/
frontend/dist/
var/
.env
*.db
*.db-journal
*.db-wal
*.db-shm
.idea/
.vscode/
data/
outbox/
*.log
# .aiball.yaml is per-user / per-machine project config (autopoll tone,
# throttle, claude-loop wiring). Use .aiball.yaml.example as the
# committed template; copy on first install. #B.166
.aiball.yaml
