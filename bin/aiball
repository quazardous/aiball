#!/usr/bin/env node
// aiball — CLI entrypoint. The command surface lives in src/cli.ts; the
// install-root / cwd / transport resolution lives in ./launcher.js, shared
// with every other command.
import { launch } from "./launcher.js";

await launch("src/cli.ts");
